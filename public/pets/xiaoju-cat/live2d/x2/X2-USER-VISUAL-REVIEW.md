# 小橘 X2 用户视觉审核包

> 状态：`x2-candidate-for-user-review`  
> 当前 Gate：X2 星球支撑与遮眼动作求解  
> 结论边界：本文只用于审核 X2，不授权 X3 母版、分层图片、三角剖分、PSD 或 Cubism。

## 必看预览

0. `qa/x2-user-review-composite.png`  
   X2 用户视觉审核总览图，把直观遮眼动作、41 采样关键帧、支撑/COM 和完整力矩审阅图合在一张图里。它只拼接已有 X2 QA 证据，不是新母版、新姿势或新图层。

0.5. `qa/x2-length-triangle-diagnostic.png`  
   X2 骨段长度与局部三角约束诊断图，用现有 41 帧块面采样检查肩胛、肩、肘、腕、爪之间是否出现骨段伸缩或体块作弊。它不是正式分层、不是正式 Delaunay、不是 ArtMesh，只是 X2 视觉诊断。

1. `qa/x2-intuitive-moment-cover-preview.png`  
   面向视觉判断的 6 格遮眼动作预览。重点看爪子是否从身体外侧自然抬起、是否遮到眼睛、是否还像小橘短前肢。

2. `qa/x2-intuitive-moment-cover-preview.gif`  
   同一动作的慢速往返预览。重点看 Rest -> Cover -> Rest 是否连续，有没有突然折返、穿脸、脱节或骨段伸缩感。

3. `qa/x2-cover-roundtrip-blockout-sheet.png`  
   41 采样中的关键帧块面图。重点看肩、肘、腕链条是否稳定，胸腹是否仍压在星球支撑带上。

4. `qa/x2-contact-support-map.png`  
   接触、支撑范围和 COM 投影。重点看双爪离开星球时，身体是否仍有可信支撑。

5. `qa/x2-full-moment-review-sheet.png`  
   肩胛、肩、肘、腕在 Cover 帧的物理力臂和 signed tau 审阅图。重点看外力/接触/重力方向传回关节的左右镜像关系是否合理。

## 自动核验摘要

- 采样数量：`41`
- 最大骨段长度漂移：`0.00000000`
- COM 全采样位于支撑范围：`true`
- Cover 附近双爪接触眼部目标区：`true`
- 峰值控制力矩需求：`0.8698`
- 峰值物理力矩需求：`1.0000`

这些自动结果只证明 X2 候选内部一致，不能代替视觉审核。

机器可读审核清单见 `x2-user-review-manifest.json`。它只记录当前 X2 审核证据、审核问题和允许的用户决策，不授权 X3。

骨段伸缩诊断见 `x2-length-triangle-diagnostic.json`。该 JSON 只汇总 X2 采样中的骨段长度漂移和局部三角边长漂移，不定义图层边界或 ArtMesh 顶点。

## 用户审核问题

- 胸腹与星球接触带是否符合登录场景意图？
- 前爪释放星球后，COM 是否仍让身体看起来稳定？
- 肩、肘、腕折叠方向是否符合小橘圆身体和短前肢？
- 爪心到眼部遮挡区的路径是否自然，是否需要左右爪错时？
- 低幅身体补偿是否足够，是否有肋笼、腹部或骨盆被压扁的风险？
- 是否确认 X2 只作为动作、支撑和力矩门禁，不在本阶段提前生成正式分层图片？

## Gate 决策

可接受的用户决策只有两类：

- `X2 通过`：才可进入 X3，生成完整连续大字型/展开三视图母版供下一轮视觉审核。
- `X2 不通过` 或指出具体问题：只回到 X2 修改支撑、遮眼块面、力矩预检和 QA 预览。

禁止把“X2 候选完成”写成“X2 已通过”。
