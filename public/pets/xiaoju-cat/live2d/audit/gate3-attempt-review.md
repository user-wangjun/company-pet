# 小橘 Gate 3 补画路线复核

> 状态：Gate 3 制作中。已创建独立工作 PSD 和可审计拆层；尚未通过隐藏解剖完整性验收，未修改 Cubism 模型或登录页。

## AI 补画尝试

四次内置图像编辑均未通过 Gate 3 身份与构图约束，因此没有复制到宠物包：

1. 第一版改成正面坐姿并保留四只可见爪。
2. 第二版只保留两只后爪，但仍重画脸部并改成正面坐姿。
3. 第三版加入轻微倾斜，仍未保持原登录透视和像素级身份。
4. 第四版以 `Cat_Body_Base` 为编辑目标，仍重画头脸、改为闭眼并改变身体姿态。

结论：停止整图生成路线。隐藏区域只允许在 Photoshop 中以原图非透明像素为锁定基准，通过蒙版、局部补画和可审计图层完成。

## 本机工具状态

- Adobe Photoshop 2026 已安装：`D:\软件\Adobe Photoshop 2026\Photoshop.exe`。
- Live2D Cubism Editor 5.3 Free 已运行。
- 原 v3 PSD 保持不变；正式工作副本为 `../gate3/xiaoju-planet-login-gate3-working.psd`。
- 已建立 `GATE3_RIG_PARTS`，包含场景、身体、头脸、眼球、双前肢关节与前景遮挡分组。
- 前肢已纠正为肩端到爪端的真实解剖顺序，并保留重叠安全边。
- 星球完整前景 alpha 已置于前肢之上，修复了前肢穿出球面的 Draw Order 错误。
- 已生成独立 `Pupil_L/R`、`Highlight_L/R` 和去原黑瞳的 `Iris_L/R_Clean`；无瞳孔 QA 已证明不存在第二个黑色瞳孔，但虹膜补片纹理仍需精修。
- 已将鼻、嘴线、口腔、下巴与左右胡须从原图像素拆成独立且重叠的可动素材；静态合成已复验，不改变批准构图。
- 全尺寸 QA：`../gate3/qa/gate3-composite.png`、`../gate3/qa/gate3-eye-base-no-pupil.png`、`../gate3/qa/gate3-layer-inventory.txt`。

## 2026-07-17 隐藏身体核验

- 尝试以原身体和前爪像素做羽化克隆，补出腹部、颈部、双髋、后腿和后爪。
- 全尺寸 `gate3-hidden-body-uncovered.png` 暴露出重复躯干轮廓、脱节后爪以及静止姿态泄漏，判定为不合格。
- 已通过 `rollback_failed_hidden_reconstruction.jsx` 从工作 PSD 原子回滚该候选；批准的遮眼静态构图恢复干净，没有把失败补画带入 Cubism。
- 当前策略改为逐区域局部重建并逐项验收；在隐藏解剖通过前，Gate 3 保持未通过状态。

## 2026-07-17 隔离 QA 修正

- 原 `gate3-hidden-body-uncovered.png` 仍显示星球，不是因为 Planet 图层未关闭，而是 `00_Stars_Background.png` 本身就是含星球的全画布构图底板。
- `export_gate3_qa.jsx` 已新增 `gate3-rig-parts-only.png`，用于证明仅 `GATE3_RIG_PARTS` 能否还原默认构图；同时在隐藏身体 QA 中关闭整个 `01_SCENE_PLANET`、前景遮挡和前肢组，禁止背景底板掩盖缺失区域。
- 修正后的展开证据明确显示：当前可用身体只包含头、颈部截面和尾巴，星球后方的完整胸腹、髋、后腿与后爪并不存在。此前任何“可进入 Cubism”的判断均不成立。
- 下一候选必须使用三视图约束完整坐姿解剖，只把生成候选中原始透明区域作为隐藏身体底材；已批准的头脸、颈、尾和其他原图非透明像素不可被生成结果覆盖。

## 当前未通过项

1. `TODO_HIDDEN_*` 仍有腹部、颈部、髋、后腿、后爪、眼眶毛、下眼睑和星球附属层，Gate 3 不能签收；口鼻、下巴和胡须拆层已经完成。
2. 虹膜安全底图虽已消除黑瞳重影，但补片仍偏平，需要加入与原虹膜一致的明暗和纤维纹理。
3. 当前 PSD 的标准构图是遮眼姿态；进入 Cubism 后必须用关节旋转链建立 `rest -> cover` 连续参数，并以五张 pose key 对照，不允许切图换帧。
4. 每完成一个区域继续生成全尺寸 QA，不等全部拆完后才检查。

## 2026-07-17 完整姿势候选复核

- `hidden-anatomy-fullbody-v1-*`：直立坐姿，颈部比例与登录图不匹配，淘汰。
- `xiaoju-fullbody-lean-v2-*`：虽生成完整躯干，但骨盆过高、后腿近乎伸直，重心落在两只后爪上，形成站立遮眼姿势，不符合猫趴靠星球的自然受力，淘汰。
- 下一版姿势硬约束：胸腹由星球边缘承重；骨盆低于肩；后腿折叠收腹；肩胛前伸、肘关节弯曲后抬腕遮眼；尾巴位于左侧平衡；不得以直立后腿承担主体重量。
- `xiaoju-prone-cover-v3-*`：趴姿重心改善，但遮眼前肢仍像两根近似平行的粗直柱，爪宽和同步性破坏视觉逻辑，淘汰为最终姿势；仅可作为胸腹/后肢结构参考。
- 已建立 `../blueprints/gate3-cover-pose-skeleton-blueprint.md` 和 `../gate3/qa/gate3-cover-pose-skeleton-guide.png`，用 Rest/Cover 双骨架、胸部接触锁和肩肘腕爪坐标约束下一轮生成。
- `xiaoju-natural-cover-v4-*`：前肢弧线有所改善，但胸腔、腹腔和骨盆被压成横向软垫，颈根塌入躯干，属于全身质量块坍缩而非局部接触压缩；同时肩胛/肱骨根部仍不成立，淘汰。
- 已建立 `../blueprints/gate3-biomechanical-volume-blueprint.md` 与 `../gate3/qa/gate3-xiaoju-volume-guide.png`；后续必须先保持头、肋笼、骨盆三质量块和胸腹接触反力，再求遮眼关节姿势。

## 2026-07-17 生物力学块面复核

- 新增 `../gate3/solve_biomechanical_pose.py`、`../gate3/qa/gate3-biomechanical-pose-blockout.png` 和机器可读报告 `../gate3/qa/gate3-biomechanical-pose-report.json`。
- 第一轮虽然骨段长度和重心检查通过，但 41 点密集审计发现左肘在 `0.425 → 0.45` 发生 IK 镜像分支翻转，单步跳变 `232.64 px`；判定不合格，证明只检查五张关键帧不足以保证生物连续性。
- 修正后双肘在全程固定同一屈曲侧，并把肩关节上移到肋笼上外侧，避免为了碰脸而把前肢拉直到接近锁死。
- 当前报告通过：最大关节单步位移 `16.84 px / 0.025 progress`，最大肘角单步变化 `3.40°`；左右肩胛位移分别 `15.81 px`、`11.18 px`；质量块缩放保持 `1.0`，最终双爪进入面部接触窗。
- 这只是姿势块面通过，不是 Gate 3 签收。下一步必须依据该块面补齐完整胸腹、骨盆、后肢以及肩胛到腕爪的独立素材，并重新做隐藏身体展开 QA。
