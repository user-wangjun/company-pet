# 小星画面右侧手臂单链验证 V2

## 当前状态

`阶段 A 骨架修正候选：等待用户视觉确认`

V1 被用户否决：肘点几乎落在肩—腕直线上，没有体现原图轮廓和人体自然下垂时的
提携角。V2 不修改或续补 V1 文件，而是重新从权威正面线稿、彩稿和三视图比例建立
单臂骨架。

本目录只验证画面右侧（角色左臂）的：

`sleeve → upper_arm → forearm → hand`

当前没有生成纯色材料、隐藏纹理、PSD 或 Cubism 工程。

## V2 核心修正

- 肩：`(332, 261)`，袖内推断点保持不变。
- 肘：由 `(368, 425)` 修正为 `(357, 425)`。
- 腕：`(393, 533)`，手链遮挡下的估计点保持不变。
- 肘点相对肩—腕直线向身体内侧偏移约 `11.78 px`。
- 默认上臂与前臂相对角为 `-9.77°`，明确采用画面右臂向外张开的同一弯曲分支。

## 通过边界

用户确认 `qa/skeleton-design-review-v2.png` 后，只冻结 V2 的肩、肘、腕、骨长、
角度范围、父子关系和 Draw Order。四个全画布纯色材料仍属于下一步。

