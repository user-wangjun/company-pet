# 小星画面右侧手臂单链验证 V4：腕部主动运动

## 当前状态

`阶段 A：工程检查与用户中文视觉审查均通过，已冻结归档`

V1 因肩—肘—腕近似直线被否决。V2 修正了横向折角，但用户指出肘关节仍偏低。
V3 不修改 V1/V2，重新从权威正面图的肘部横截面和上下骨段比例求解肘高。

## V3 修正

- 肩：`(332,261)`
- 肘：由 V2 的 `(357,425)` 上移并内移到 `(355,415)`
- 腕：`(393,533)`
- 上臂骨长：`155.708 px`
- 前臂骨长：`123.968 px`
- 上臂/前臂比：`1.256`
- 默认相对肘角：`-9.356°`

V4 复用 V3 已审查的骨架点、固定 L1/L2 与四材料几何，只修正腕节点虽然存在但
没有独立角度的问题。尚未生成真实隐藏纹理、PSD 或 Cubism 工程。

## 用户门禁

V3 的批准只绑定 V3 产物摘要。V4 已重新通过腕部运动工程检查与用户视觉审查，
不继承旧摘要；当前结论以 V4 归档清单为准。

## 阶段 A 当前结果

- 41 个 `0→1→0` 样本和 4 个组合极值已生成。
- 固定骨长误差、接缝覆盖、材料连通、原型三角形方向与确定性回程均通过工程检查。
- 视觉证据位于 `qa/`，工程报告位于 `audit/stage-a-geometry-report.*`。
- 腕点仍由前臂 FK 确定，L1/L2 不变。
- 新增主动参数 `phiWristLocal`，主路径为 `0°→10°→0°`，允许范围 `-8°..12°`。
- 无 Physics；手部只绕腕点局部旋转。
- 回归记录见 `audit/stage-a-regression-wrist-motion.json`。
- 当前只解锁阶段 B 的一处“袖子下面的上臂隐藏纹理”验证。
- 归档清单与下一阶段交接位于 `archive/`。
