# V7 预冻结逐项证据审计

## 结论

工程证据已完整，当前不能冻结，唯一缺项是用户对 V7-C 新视觉证据和肘根合同替换的明确批准。

## 已证明

- 冻结输入：V4 `17/17`、完整袖子 `16/16`、V6 `22/22`；
- 可见前臂：`3756 px`，坐标差 `0`，RGB 差 `0`；
- 正式前臂：`3890 px`，连通分量 `1`，孔洞 `0`；
- F1–F4：两两重叠 `0`，重建差 `0`；
- 临时手根：`389 px`，连通分量 `1`，孔洞 `0`，源轮廓外 `0`，种子缺失 `0`；
- 正式肘部：`41+6+241`，缺口 `0`，断开 `0`，回程差 `0`，骨长误差 `0`；
- 腕部：201 点密扫、临界 `±ε`、自适应极小值搜索，缺口 `0`，断开 `0`，回程差 `0`；
- 肘×腕：`9×9`，参数角点齐全，缺口 `0`；
- 确定性：V7-C 24 个非清单文件复跑逐字节一致；
- 对外隐私扫描：`0` 命中；
- 未制作纹理、PSD、ArtMesh、Cubism、Physics 或 Runtime。

## 合同例外

原始 V6 临时前臂最低代理不能同时满足中性源所有权与已批准轮廓，因此没有声称正式前臂包含旧代理，也没有修改 V6 文件。

替代合同为：

`源像素写保护的 F1 动态肘根 + 用户已批准的连续外侧收窄 + 正式前臂全域结果证明`

这项替代仍需在 V7-C 视觉门禁中由用户明确批准。

## 当前唯一停止条件

`用户尚未批准当前 V7-C 视觉门禁`

批准前：

- 不生成冻结清单；
- 不把工程通过写成最终完成；
- 不进入正式手部、纹理或 Cubism。

批准入口：

`v7c-production-forearm-geometry/audit/V7-C-USER-REVIEW-CHECKLIST.zh-CN.md`
