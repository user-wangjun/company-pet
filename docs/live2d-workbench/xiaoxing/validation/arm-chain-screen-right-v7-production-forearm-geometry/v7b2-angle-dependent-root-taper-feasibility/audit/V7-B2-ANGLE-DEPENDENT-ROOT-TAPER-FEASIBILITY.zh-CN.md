# V7-B2 角度相关前臂根部收窄可行性

## 结论

本轮状态：`engineering_preflight_pass_pending_user_visual_review`。

这只是经用户授权的纯色形状预检，不是 Cubism、ArtMesh，也不是正式前臂通过。它验证的是：保留已经批准的中性像素，进入更弯的肘角分支时，前臂外侧根部能否连续压窄并确定性复原。

## 形变定义

- 中性肘角：`-9.355943°`；
- 最弯肘角：`-27.355943°`；
- 激活函数：`smoothstep((rest-theta)/(rest-min))`，中性及近伸直侧为 0；
- 最大外侧边界位移：`4.373771 px`；
- 纵向作用范围：前臂局部 `s ∈ [0,22.0] px`，在末端连续衰减至 0；
- 只压缩局部法线正侧；局部 `s` 不变，因此肘点、骨长和腕点不靠缩短掩盖问题；
- 最小正侧比例：`0.757076`，大于 0，不发生映射折叠。

## 数值证据

- 中性遮罩差异：`0` 个 4× 子像素；
- 中性激活：`0.0`；
- `0→1→0` 回程差异：`0` 个 4× 子像素；
- 最弯根宽：`31.511341 px`；
- 冻结上臂末端参考宽：`31.409473 px`；
- 最弯宽差：`0.101868 px`；
- 全扫描责任区最大透明缺口：`0` 个 4× 子像素；
- 断开样本：`0`；
- 最大相邻样本根宽跳变：`0.035598 px`；
- 弯曲分支根宽单调：`true`；
- 最大局部裁切窗口面积损失率：`0.027919`。

扫描覆盖 41 个主路径样本、6 个组合极值和 241 点肘角密扫。重复角度保留在汇总中，密扫明细单独记录。

## 当前裁决

该可行性块满足“中性完全不变、最弯连续收窄约 4.4 px、无责任区透明缺口、可确定性回程”的工程预检条件。它只说明这种形变策略值得进入用户视觉审查；用户仍需查看慢扫是否自然，尤其是最弯处是否仍有凸出、压扁或跳变。

V7-B0 的紫色独立 `H_root` 已保持否决，不在本候选中恢复。手链仍合并在前臂材料，不建立独立运行层。

## 尚未完成

- 没有生成正式 F1/F2/F3/F4 前臂完整材料；
- 没有建立并验证临时手部最低根部包络；
- 没有完成腕角密扫、肘×腕组合域、内部极小值和低余量自适应加密；
- 没有完成正式前臂全套中文视觉证据；
- 没有冻结 V7 正式前臂；
- 没有制作纹理、PSD、Cubism、Physics 或 Runtime。

因此，最初 V7 任务尚未完成。
