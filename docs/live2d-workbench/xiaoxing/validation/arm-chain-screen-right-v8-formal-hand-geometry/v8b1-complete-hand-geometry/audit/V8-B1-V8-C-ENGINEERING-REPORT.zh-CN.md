# V8-B1 正式完整手部几何与 V8-C 真实腕部验证

状态：`engineering_pass_pending_user_visual_approval`。数值通过不替代用户视觉批准。

## 正式材料

- H1：`2629 px`，坐标差 `0`，RGB 差 `0`；
- H2：`81 px`，精确等于 `E_hand \ H1`；
- H3：`42 px`；逆覆盖栅格下界 `0 px`，采用腕根宽度剖面向前臂侧延伸 `2.0 px`，
  其中 AA 余量 `1.0 px`、未来网格变形余量 `1.0 px`；
- M_hand：`2752 px`，连通分量 `1`，孔洞 `0`；
- 389 px 包络缺失：`0`；
- 指缝误填：`0`。

## V8-C

- 腕部 `201 + 11 + 81`：最大皮肤责任缺口 `0`（4x），断开
  `0`；
- 肘×腕 `9×9`：最大缺口 `0`（4x），
  断开 `0`；
- 未批准前臂碰撞 `0`；
- 上臂/袖子碰撞 `0`；
- 原型三角形翻折 `0`；
- 最大骨长误差 `0.000000000 px`；
- `0→1→0` 回程差 `0`；
- 手链未计入皮肤覆盖。

## 当前边界

工程结果仅等待用户审查 `qa/V8-B1-V8-C-USER-REVIEW-BOARD.zh-CN.png` 与各项
明细证据。用户批准前不冻结，不进入纹理、PSD、ArtMesh、Cubism、Physics 或 Runtime。
